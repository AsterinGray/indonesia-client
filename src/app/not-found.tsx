import "@/styles/pages/not-found.module.scss";

const NotFound = () => (
  <section className="not-found">
    <h1>Page Not Found</h1>
    <p>Could not find requested page.</p>
  </section>
);

export default NotFound;
